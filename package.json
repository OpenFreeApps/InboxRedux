{
  "name": "inboxredux",
  "version": "0.2.1",
  "private": true,
  "description": "Build helpers for the InboxRedux Firefox and Chrome extension packages.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:firefox": "node scripts/build.mjs firefox",
    "build:chrome": "node scripts/build.mjs chrome"
  }
}
